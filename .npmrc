# React Native + Metro need a flat node_modules layout to resolve
# transitive deps like `invariant`, `@babel/runtime`, etc. pnpm's strict
# symlinked store breaks that. Switching to a hoisted (npm-style) linker
# keeps pnpm's workspace + lockfile benefits without the resolver pain.
node-linker=hoisted
