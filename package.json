{
  "name": "deedspan",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "project": "concurrently --kill-others --names API,WEB \"npm run dev:api\" \"npm run dev:web\"",
    "dev:web": "npm --prefix apps/web run dev",
    "dev:api": "cd apps/api && uv run uvicorn app.main:app --reload --port 8000",
    "dev:mobile": "pnpm --filter @deedspan/mobile start",
    "build:web": "pnpm --filter @deedspan/web build"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "concurrently": "9.2.1",
    "react": "19.2.6",
    "react-dom": "19.2.6"
  }
}
